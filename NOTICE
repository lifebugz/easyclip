EasyClip
Copyright (c) 2026 lifebugz
Licensed under the MIT License (see LICENSE).

EasyClip's own source code is licensed under the MIT License. It bundles the
third-party software below as separate sidecar executables, invoked as child
processes over a command-line interface and NOT linked (statically or
dynamically) into the EasyClip binary. Because these are independent programs
run at arm's length ("mere aggregation"), EasyClip's own source code is not a
derivative work of FFmpeg and remains under the MIT License. The MIT License
covers EasyClip's own source ONLY — not the distributed bundle as a whole (see
"For redistributors" below).

  FFmpeg and ffprobe — the FFmpeg 8.1 series (the shipped macOS and Linux
  builds are 8.1.2), unmodified prebuilt binaries from the build projects
  named below.
  Copyright (c) the FFmpeg developers.

  The license of the bundled FFmpeg build depends on the platform:

    - Windows (x86_64 and ARM64): GNU Lesser General Public License,
      version 2.1 or later (LGPL-2.1-or-later). BtbN "lgpl" build, configured
      WITHOUT --enable-gpl / --enable-nonfree (excludes libx264 / libx265).

    - macOS (arm64 and x86_64) and Linux (x86_64): GNU General Public License,
      version 3 or later (GPL-3.0-or-later). ffmpeg.martin-riedl.de build,
      configured WITH --enable-gpl and --enable-version3 (statically links
      libx264, libx265 and other GPL-compatible libraries). EasyClip performs
      lossless stream copy only ("-c copy") and never invokes these encoders,
      but the binaries shipped on macOS and Linux are nonetheless
      GPL-3.0-licensed.

  For redistributors: the MIT License above covers EasyClip's own source code
  ONLY, not the bundle as a whole. The macOS and Linux builds embed a
  GPL-3.0-or-later FFmpeg; if you redistribute those builds you are conveying
  GPL-3.0 software and must comply with the GPL yourself — retain these
  license texts and pass the Corresponding Source (or the written offer
  below) to your recipients. The Windows build embeds an LGPL-2.1 FFmpeg and
  carries the analogous LGPL duties.

  Corresponding Source: for a GPL/LGPL FFmpeg binary, the Corresponding Source
  is the FFmpeg source PLUS the source of every statically-linked GPL/LGPL
  library in that build, plus the scripts used to build it. For the macOS
  and Linux GPL builds that include (among others) libx264 and libx265.
  Upstream sources:
    - FFmpeg 8.1:        https://ffmpeg.org/download.html
    - x264:              https://code.videolan.org/videolan/x264
    - x265:              https://bitbucket.org/multicoreware/x265_git
    - macOS/Linux build config/scripts: https://ffmpeg.martin-riedl.de/
    - Windows LGPL build:         https://github.com/BtbN/FFmpeg-Builds
  The exact builds and their SHA-256 checksums are pinned in
  scripts/ffmpeg-checksums.json (the Windows LGPL archives and the
  shipped macOS arm64 / Linux x86_64 GPL archives are also mirrored at
  https://github.com/lifebugz/easyclip/releases).

  Written offer (the authoritative source mechanism): for three (3) years from
  the date of distribution, open an issue at
  https://github.com/lifebugz/easyclip/issues and we will provide the complete
  Corresponding Source for the exact bundled build — or a download location for
  it — at no more than the cost of distribution.

  Modified FFmpeg / replacement: because FFmpeg is shipped as a separate
  executable, you may run your own build by replacing the sidecar binary at
  src-tauri/binaries/ffmpeg-<target-triple> (or the corresponding file inside
  the installed application; on macOS this may invalidate the app's code
  signature). For an unmodified, separately-shipped (unlinked) executable the
  applicable LGPL duties are the notices and source/offer above — the LGPL's
  relink clause is not separately triggered.

Full third-party license texts — the GNU LGPL v2.1 (Windows sidecar) and the
GNU GPL v3 (macOS and Linux sidecars) — are in THIRD-PARTY-LICENSES.md, which
is bundled with the application on every platform.
